# Audit workflow — runs the UI/UX audit harness against the dev server.
# Weekly cron + manual dispatch. Cheap (Ubuntu, no Wails build required)
# because dixiedata-web runs as a Linux binary and the audit scripts
# target the dev server.

name: audit

on:
  schedule:
    # Mondays 06:00 UTC
    - cron: '0 6 * * 1'
  workflow_dispatch:
  pull_request:
    branches: [dev, stable]
    paths:
      - 'internal/**'
      - 'pkg/**'
      - 'cmd/**'
      - 'frontend/**'
      - 'audit/**'
      - '.github/workflows/audit.yml'

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install npm deps
        run: npm ci

      - name: Install Playwright browser
        run: npx playwright install chromium --with-deps

      - name: Regenerate templ files
        # internal/templates/*_templ.go is gitignored (generated
        # from .templ source files), so a fresh CI checkout has
        # only the .templ files plus the plain .go files. The plain
        # .go files (e.g. internal/templates/linked_text_support.go)
        # call helpers like `isSoldierEntry` that are defined in
        # the generated `*_templ.go` files. Without this step the
        # go build below fails with "undefined: isSoldierEntry".
        # The test workflow already runs this step; audit was
        # missing it. (Discovered 2026-06-29 when verifying the
        # pkg/render build-tag fix on dev.)
        run: |
          go run github.com/a-h/templ/cmd/templ@v0.3.1001 generate
        timeout-minutes: 5

      - name: Build dixiedata-web + seed-data
        run: |
          mkdir -p build/bin
          go build -o build/bin/dixiedata-web ./cmd/dixiedata-web
          go build -o build/bin/seed-data ./cmd/seed-data

      - name: Seed sample archive (50 soldiers)
        run: |
          mkdir -p .scratch/webmode
          ./build/bin/seed-data -data-dir .scratch/webmode -soldiers 50 -reset

      - name: Start dev server
        run: |
          ./build/bin/dixiedata-web -addr 127.0.0.1:8080 -scratch-dir .scratch/webmode &
          # Poll for readiness up to 30s instead of fixed sleep
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/ > /dev/null; then
              echo "Server ready after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "Server failed to start within 30s"
          exit 1

      # Issue #456 follow-up: the Share top-nav foldout is one of
      # the most prominent UI surfaces and was once blanked by a
      # templ codegen bug (children block dropped on the floor).
      # Gate the children-rendering invariant in CI via the
      # Playwright probe that already encodes the contract — it
      # asserts `panel.itemCount === 4` and the click-to-open +
      # ARIA + navigation behaviors. The probe accepts BASE_URL
      # so it reuses the dev server already running on :8080; no
      # second server is spawned.
      # Issue #380 slice 3 collapsed the 2 prior top-nav foldouts
      # (Research & Review + Share) + the standalone Insights pill
      # into a single 2D Share & Review mega-menu. The probe gates
      # the post-#380 mega-menu ARIA contract + click-outside + first-click
      # regressions + WCAG AA contrast on every menuitem.
      # Originally issue #456 gated the pre-#380 foldout via
      # smoke_foldout_nav.mjs; that probe was deleted when the foldout
      # primitive was retired in #380 slice 3 and replaced with the
      # mega-menu primitive audited here.
      - name: Mega-menu regression probe (issue #380 slice 3)
        run: BASE_URL=http://127.0.0.1:8080 node audit/smoke_mega_menu_nav.mjs
        timeout-minutes: 5

      - name: Run audit harness
        run: |
          mkdir -p audit/reports-ci
          node audit/run.mjs --output-dir audit/reports-ci 2>&1 | tee audit/reports-ci/round1.log
          node audit/run-round2.mjs --output-dir audit/reports-ci 2>&1 | tee audit/reports-ci/round2.log
          node audit/run-round3.mjs --output-dir audit/reports-ci 2>&1 | tee audit/reports-ci/round3.log

      - name: Upload audit reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audit-reports
          path: |
            audit/reports-ci/
            audit/reports/
            audit/reports-r2/
            audit/reports-r3/
          retention-days: 30
