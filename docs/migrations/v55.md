# v55 — Image compression metadata

- Add `compressed_at DATETIME`, `original_bytes INTEGER DEFAULT 0`,
  `compressed_bytes INTEGER DEFAULT 0` to the `images` table to track
  per-image compression status and savings. See issue #73.
