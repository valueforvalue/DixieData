# v52 — Chicago timezone enforcement (no-op schema change)

- No schema-level changes in this release.
- `CurrentSchemaVersion` bumped from 51 to 52 to track the release line.
- Behavioral change: managed and test Google calendars, plus iCal export, now
  use an explicit `America/Chicago` timezone rather than relying on the
  system clock. Calendar-event timestamps display consistently regardless of
  the host machine's local timezone.
- Manual steps: none. Existing archives continue to migrate without
  intervention because the schema is unchanged.
- See `git log v1.2.51..v1.2.52` for the commit range.
