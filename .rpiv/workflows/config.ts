/**
 * Workflow config — generated by my-skills-framework bootstrap.
 * Edit if you need to override bucket paths or descriptions.
 */

import { makePlanBuild, makeDesignItTwice, makeShipIssue, makeDiagnoseBug, makeRefactorPlan, makePrTriage, makeQaTriage, makeArchitectureReview } from "C:/Development/my-skills-framework/workflows.ts";

export default {
  workflows: {
    "plan-build": makePlanBuild({ root: ".rpiv/artifacts" }),
    "design-it-twice": makeDesignItTwice({ root: ".rpiv/artifacts" }),
    "ship-issue": makeShipIssue({ root: ".rpiv/artifacts" }),
    "diagnose-bug": makeDiagnoseBug({ root: ".rpiv/artifacts" }),
    "refactor-plan": makeRefactorPlan({ root: ".rpiv/artifacts" }),
    "pr-triage": makePrTriage({ root: ".rpiv/artifacts" }),
    "qa-triage": makeQaTriage({ root: ".rpiv/artifacts" }),
    "architecture-review": makeArchitectureReview({ root: ".rpiv/artifacts" }),
  },
  default: "plan-build",
};
