/**
 * Workflow config — generated by my-skills-framework bootstrap.
 * Edit if you need to override bucket paths or descriptions.
 *
 * Shape: `{ workflows: Workflow[], default?: string }` per
 * @juicesharp/rpiv-workflow's loader contract. Each factory
 * returns a fully-defined `Workflow`; the array is the
 * "registered workflows" namespace, and `default` selects the
 * one `/wf` runs with no explicit `--name`.
 */

import {
  makePlanBuild,
  makeDesignItTwice,
  makeShipIssue,
  makeDiagnoseBug,
  makeRefactorPlan,
  makePrTriage,
  makeQaTriage,
  makeArchitectureReview,
} from "C:/Development/my-skills-framework/workflows.ts";

const ARTIFACT_ROOT = ".rpiv/artifacts";

export default {
  workflows: [
    makePlanBuild({ root: ARTIFACT_ROOT }),
    makeDesignItTwice({ root: ARTIFACT_ROOT }),
    makeShipIssue({ root: ARTIFACT_ROOT }),
    makeDiagnoseBug({ root: ARTIFACT_ROOT }),
    makeRefactorPlan({ root: ARTIFACT_ROOT }),
    makePrTriage({ root: ARTIFACT_ROOT }),
    makeQaTriage({ root: ARTIFACT_ROOT }),
    makeArchitectureReview({ root: ARTIFACT_ROOT }),
  ],
  default: "plan-build",
};
